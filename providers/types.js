const ProviderIds = Object.freeze({
  MOCK: 'mock',
  OLLAMA: 'ollama',
  OPENROUTER: 'openrouter',
  GEMINI: 'gemini',
  GROQ: 'groq'
});

const Models = Object.freeze({
  MOCK_BRAIN_V1: 'mock-brain-v1'
});

module.exports = {
  ProviderIds,
  Models
};
